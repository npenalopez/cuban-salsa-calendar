
  # Interactive Calendar

  This is a code bundle for Interactive Calendar. The original project is available at https://www.figma.com/design/dvhSsnzVGCNc5r9hGIDDsh/Interactive-Calendar.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  